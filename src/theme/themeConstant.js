import { createMuiTheme } from "@material-ui/core";
import { purple, pink, orange, red, blue, green } from "@material-ui/core/colors";
import { dark } from "@material-ui/core/styles/createPalette";


export const theme = createMuiTheme({
  palette: {
    primary: purple,
    secondary: green,
  }
})